# Class (static) Variables & Methods

**Outline ref:** 7.4 — Class (static) Variables and Methods (CSA 3.7)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 7.4 · static.variables.and.methods()</span>
  <h2>Class (static) <span class="y">Variables & Methods</span></h2>
  <p class="lead">A <span class="k">static</span> member belongs to the class itself — one shared copy, no matter how many objects exist. A static method can't touch instance fields without being handed an actual object.</p>
  <div class="code">
    <span class="cl"><span class="k">public class</span> <span class="t">Subsystem</span> {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">public static int</span> instanceCount = <span class="n">0</span>; <span class="c">// shared by every Subsystem</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">public</span> <span class="me">Subsystem</span>() { instanceCount++; }</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-3-class-creation/3.07-class-static-variables-and-methods.md` (CSA 3.7)
