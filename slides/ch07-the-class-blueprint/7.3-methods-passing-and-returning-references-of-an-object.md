# Passing & Returning Object References

**Outline ref:** 7.3 — Methods: Passing/Returning References of an Object (CSA 3.6)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 7.3 · passing.and.returning.references()</span>
  <h2>Passing & Returning <span class="y">Object References</span></h2>
  <div class="two">
    <div>
      <p class="lead">Java passes a <em>copy of the reference</em>, not the object itself — a method that mutates the object you passed in changes the one and only real copy everyone shares.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>If you don't want a method to be able to change your object, pass (or return) a defensive copy instead of the original.</span></li>
        <li><span class="bul">▸</span><span>Chaining calls like <span class="me">drivetrain.getPose().getX()</span> only works if you know exactly what type each method returns.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">public void</span> <span class="me">setPose</span>(<span class="t">Pose2d</span> newPose) {</span>
      <span class="cl">&nbsp;&nbsp;<span class="c">// stores the SAME reference the caller passed in</span></span>
      <span class="cl">&nbsp;&nbsp;<span class="k">this</span>.pose = newPose;</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-3-class-creation/3.06-methods-passing-and-returning-references-of-an-object.md` (CSA 3.6)
