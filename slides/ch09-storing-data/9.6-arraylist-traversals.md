# ArrayList Traversals

**Outline ref:** 9.6 — ArrayList Traversals (CSA 4.9)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 9.6 · arraylist.traversals()</span>
  <h2>ArrayList <span class="y">Traversals</span></h2>
  <div class="two">
    <div>
      <p class="lead">Same loop shapes as arrays, but with <span class="me">.get(i)</span>/<span class="me">.size()</span> instead of <span class="k">[]</span>/<span class="k">.length</span>.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Never add or remove elements while inside a for-each loop over the list — that throws <span class="t">ConcurrentModificationException</span>.</span></li>
        <li><span class="bul">▸</span><span>Removing during an indexed loop shifts everything down — don't advance the index after a removal, or you'll skip the element that shifted in.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="t">ArrayList</span>&lt;<span class="t">String</span>&gt; faultLog = <span class="k">new</span> <span class="t">ArrayList</span>&lt;&gt;();</span>
      <span class="cl"><span class="k">for</span> (<span class="t">String</span> fault : faultLog) {</span>
      <span class="cl">&nbsp;&nbsp;System.out.<span class="me">println</span>(fault);</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.09-arraylist-traversals.md` (CSA 4.9)
