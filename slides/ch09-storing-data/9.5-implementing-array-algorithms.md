# Implementing Array Algorithms

**Outline ref:** 9.5 — Implementing Array Algorithms (CSA 4.5)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 9.5 · array.algorithms()</span>
  <h2>Implementing Array <span class="y">Algorithms</span></h2>
  <p class="lead">Search, min/max, and duplicate-detection all follow the same shape: a loop with an <span class="k">if</span> inside — check every element before concluding anything wasn't found.</p>
  <div class="code">
    <span class="cl"><span class="k">double</span> maxCurrent = motorCurrents[<span class="n">0</span>];</span>
    <span class="cl"><span class="k">for</span> (<span class="k">double</span> amps : motorCurrents) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">if</span> (amps > maxCurrent) maxCurrent = amps;</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.05-implementing-array-algorithms.md` (CSA 4.5)
