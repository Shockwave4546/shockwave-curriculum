# Using Text Files

**Outline ref:** 9.8 — Using Text Files (CSA 4.6)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 9.8 · using.text.files()</span>
  <h2>Using <span class="y">Text Files</span></h2>
  <div class="two">
    <div>
      <p class="lead"><span class="t">Scanner</span> + <span class="t">File</span> reads data that persists across runs — useful for loading autonomous paths, config, or tuning values without recompiling.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Must handle <span class="t">IOException</span> — the file might not exist.</span></li>
        <li><span class="bul">▸</span><span><span class="me">.split(",")</span> turns one line of a CSV into an array of fields.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="t">File</span> config = <span class="k">new</span> <span class="t">File</span>(<span class="s">"tuning.csv"</span>);</span>
      <span class="cl"><span class="t">Scanner</span> scan = <span class="k">new</span> <span class="t">Scanner</span>(config);</span>
      <span class="cl"><span class="k">while</span> (scan.<span class="me">hasNext</span>()) {</span>
      <span class="cl">&nbsp;&nbsp;<span class="t">String</span>[] fields = scan.<span class="me">nextLine</span>().<span class="me">split</span>(<span class="s">","</span>);</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.06-using-text-files.md` (CSA 4.6)
