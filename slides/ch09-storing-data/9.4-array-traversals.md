# Array Traversals

**Outline ref:** 9.4 — Array Traversals (CSA 4.4)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 9.4 · array.traversals()</span>
  <h2>Array <span class="y">Traversals</span></h2>
  <div class="two">
    <div>
      <p class="lead">Loop from index 0 to <span class="k">length - 1</span> to visit every element — use an enhanced for-each when you just need the values, not the index.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Arrays are objects — pass one into a method and changes made there affect the original.</span></li>
        <li><span class="bul">▸</span><span>For-each can't reassign array elements — need an indexed loop if you're modifying values.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">double</span>[] moduleAngles = <span class="k">new</span> <span class="k">double</span>[<span class="n">4</span>];</span>
      <span class="cl"><span class="k">for</span> (<span class="k">double</span> angle : moduleAngles) {</span>
      <span class="cl">&nbsp;&nbsp;System.out.<span class="me">println</span>(angle);</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.04-array-traversals.md` (CSA 4.4)
