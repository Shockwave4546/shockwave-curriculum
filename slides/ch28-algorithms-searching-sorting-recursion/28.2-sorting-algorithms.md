# Sorting Algorithms

**Outline ref:** 28.2 — Sorting Algorithms (CSA 4.15)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 28.2 · sorting.algorithms()</span>
  <h2>Sorting <span class="y">Algorithms</span></h2>
  <div class="two">
    <div>
      <p class="lead"><strong>Selection sort</strong> repeatedly finds the minimum and swaps it into place. <strong>Insertion sort</strong> inserts each value where it belongs among the already-sorted elements.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Sorting detected game pieces by distance before picking the closest one — either algorithm gets you there.</span></li>
        <li><span class="bul">▸</span><span>Insertion sort is faster on nearly-sorted data, like a list that's re-sorted every loop after one new detection arrives.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">for</span> (<span class="k">int</span> i = 1; i &lt; pieces.length; i++) {</span>
      <span class="cl">&nbsp;&nbsp;<span class="c">// shift closer pieces right until this one fits</span></span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.15-sorting-algorithms.md` (CSA 4.15)
