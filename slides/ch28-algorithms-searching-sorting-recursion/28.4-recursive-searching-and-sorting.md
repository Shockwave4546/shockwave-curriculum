# Recursive Searching and Sorting

**Outline ref:** 28.4 — Recursive Searching and Sorting (CSA 4.17)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 28.4 · recursive.search.and.sort()</span>
  <h2>Recursive <span class="y">Search & Sort</span></h2>
  <div class="two">
    <div>
      <p class="lead">Binary search and merge sort can both be written recursively — each call works on a smaller slice of the same array until it hits a base case.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Recursive binary search: two base cases — found the middle, or the range is empty.</span></li>
        <li><span class="bul">▸</span><span>Merge sort splits until each piece is one element, then merges sorted halves back together — <span class="t">O(n log n)</span>, faster than selection/insertion sort at scale.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">int</span> mid = (start + end) / 2;</span>
      <span class="cl"><span class="k">if</span> (target == ids[mid]) <span class="k">return</span> mid;</span>
      <span class="cl"><span class="k">if</span> (end &lt; start) <span class="k">return</span> -1;</span>
      <span class="cl"><span class="k">return</span> target &lt; ids[mid]</span>
      <span class="cl">&nbsp;&nbsp;? <span class="me">recursiveBinarySearch</span>(ids, start, mid - 1, target)</span>
      <span class="cl">&nbsp;&nbsp;: <span class="me">recursiveBinarySearch</span>(ids, mid + 1, end, target);</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.17-recursive-searching-and-sorting.md` (CSA 4.17)
