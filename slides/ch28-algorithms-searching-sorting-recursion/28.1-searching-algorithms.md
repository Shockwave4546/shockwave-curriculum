# Searching Algorithms

**Outline ref:** 28.1 — Searching Algorithms (CSA 4.14)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 28.1 · searching.algorithms()</span>
  <h2>Searching <span class="y">Algorithms</span></h2>
  <div class="two">
    <div>
      <p class="lead"><strong>Linear search</strong> checks every element until it finds a match. <strong>Binary search</strong> only works on sorted data, but cuts the search space in half each step.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Finding the nearest AprilTag ID in an unsorted detection list? Linear search — you can't assume order.</span></li>
        <li><span class="bul">▸</span><span>Looking up a tag ID in a sorted field-layout table? Binary search wins as the table grows.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">for</span> (<span class="t">AprilTag</span> tag : detections) {</span>
      <span class="cl">&nbsp;&nbsp;<span class="k">if</span> (tag.<span class="me">getId</span>() == target) <span class="k">return</span> tag;</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.14-searching-algorithms.md` (CSA 4.14)
