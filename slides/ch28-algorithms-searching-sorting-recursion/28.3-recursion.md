# Recursion

**Outline ref:** 28.3 — Recursion (CSA 4.16)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 28.3 · recursion()</span>
  <h2><span class="y">Recursion</span></h2>
  <p class="lead">A method that calls itself, working toward a <strong>base case</strong> that stops it. You've already seen this shape: command compositions (<span class="me">andThen</span>, <span class="me">raceWith</span>) nest recursively, just like a recursive method call.</p>
  <div class="code">
    <span class="cl"><span class="k">public static int</span> <span class="me">countStages</span>(<span class="t">Command</span> c) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">if</span> (!c.<span class="me">hasChildren</span>()) <span class="k">return</span> 1; <span class="c">// base case</span></span>
    <span class="cl">&nbsp;&nbsp;<span class="k">return</span> 1 + <span class="me">countStages</span>(c.<span class="me">getNext</span>()); <span class="c">// recursive call</span></span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.16-recursion.md` (CSA 4.16)
