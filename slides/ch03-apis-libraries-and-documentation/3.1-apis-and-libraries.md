# APIs & Libraries

**Outline ref:** 3.1 — APIs and Libraries (CSA 1.7)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 3.1 · apis.and.libraries()</span>
  <h2>APIs & <span class="y">Libraries</span></h2>
  <p class="lead">A <strong>library</strong> is code someone else wrote that you can reuse. Its <strong>API</strong> (Application Programming Interface) tells you how to use it — what classes and methods it offers — without needing to know how it works internally.</p>
  <div class="two">
    <ul class="bullets">
      <li><span class="bul">▸</span><span>WPILib itself is a library — thousands of classes for motors, sensors, and control loops you never have to write from scratch.</span></li>
      <li><span class="bul">▸</span><span>A method's Javadocs describe its <strong>signature</strong> — what it needs and what it returns — before you ever read its source code.</span></li>
    </ul>
    <div class="code">
      <span class="cl"><span class="c">// Using WPILib's own API — you don't need to know</span></span>
      <span class="cl"><span class="c">// how PIDController computes its output internally</span></span>
      <span class="cl"><span class="t">PIDController</span> pid = <span class="k">new</span> <span class="t">PIDController</span>(<span class="n">0.1</span>, <span class="n">0</span>, <span class="n">0</span>);</span>
      <span class="cl"><span class="k">double</span> output = pid.<span class="me">calculate</span>(currentPos, target);</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.07-apis-and-libraries.md` (CSA 1.7)
