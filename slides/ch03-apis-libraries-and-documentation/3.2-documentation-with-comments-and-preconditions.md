# Documentation & Preconditions

**Outline ref:** 3.2 — Documentation with Comments and Preconditions (CSA 1.8)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 3.2 · documentation()</span>
  <h2>Documentation & <span class="y">Preconditions</span></h2>
  <div class="two">
    <div>
      <p class="lead">Comments explain code to programmers, not the compiler. A <strong>precondition</strong> is what a method expects to be true before it runs correctly — the method doesn't check it for you.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span><span class="k">//</span> single-line, <span class="k">/* */</span> multi-line, <span class="k">/** */</span> generates real API docs.</span></li>
        <li><span class="bul">▸</span><span><span class="me">Math.sqrt(-1)</span> doesn't crash — it just quietly returns <span class="t">NaN</span>, since its precondition (non-negative input) wasn't met.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="c">/**</span></span>
      <span class="cl"><span class="c"> * Sets the flywheel target speed.</span></span>
      <span class="cl"><span class="c"> * Precondition: speed must be between 0.0 and 1.0.</span></span>
      <span class="cl"><span class="c"> */</span></span>
      <span class="cl"><span class="k">public void</span> <span class="me">setSpeed</span>(<span class="k">double</span> speed) { ... }</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.08-documentation-with-comments-and-preconditions.md` (CSA 1.8)
