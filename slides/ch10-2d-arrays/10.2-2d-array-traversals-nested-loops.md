# 2D Array Traversals: Nested Loops

**Outline ref:** 10.2 — 2D Array Traversals: Nested Loops (CSA 4.12)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 10.2 · 2d.array.traversals()</span>
  <h2>2D Array Traversals: <span class="y">Nested Loops</span></h2>
  <p class="lead">Row-major or column-major — pick which index varies in the outer loop. Rows × columns total passes through the body.</p>
  <div class="code">
    <span class="cl"><span class="k">for</span> (<span class="k">int</span> row = <span class="n">0</span>; row < grid.length; row++) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">for</span> (<span class="k">int</span> col = <span class="n">0</span>; col < grid[<span class="n">0</span>].length; col++) {</span>
    <span class="cl">&nbsp;&nbsp;&nbsp;&nbsp;System.out.<span class="me">print</span>(grid[row][col] + <span class="s">" "</span>);</span>
    <span class="cl">&nbsp;&nbsp;}</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.12-2d-array-traversals-nested-loops.md` (CSA 4.12)
