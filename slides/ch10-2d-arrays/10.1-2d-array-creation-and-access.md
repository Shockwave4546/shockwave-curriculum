# 2D Array Creation & Access

**Outline ref:** 10.1 — 2D Array Creation and Access (CSA 4.11)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 10.1 · 2d.array.creation()</span>
  <h2>2D Array <span class="y">Creation & Access</span></h2>
  <p class="lead">A 2D array is really an array of arrays — <span class="k">[row][col]</span> indexes into it, both starting at 0. Perfect for anything grid-shaped: a vision-field occupancy map, an odometry covariance matrix.</p>
  <div class="code">
    <span class="cl"><span class="k">double</span>[][] occupancyGrid = <span class="k">new</span> <span class="k">double</span>[<span class="n">8</span>][<span class="n">8</span>];</span>
    <span class="cl">occupancyGrid[<span class="n">3</span>][<span class="n">5</span>] = <span class="n">1.0</span>; <span class="c">// mark a cell occupied</span></span>
    <span class="cl">System.out.<span class="me">println</span>(occupancyGrid.length + <span class="s">" rows"</span>);</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-4-data-collections/4.11-2d-array-creation-and-access.md` (CSA 4.11)
