# Casting & Ranges of Values

**Outline ref:** 2.4 — Casting and Ranges of Values (CSA 1.5)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 2.4 · casting.and.ranges()</span>
  <h2>Casting & <span class="y">Ranges of Values</span></h2>
  <div class="two">
    <div>
      <p class="lead">A cast — <span class="k">(int)</span> or <span class="k">(double)</span> — reshapes a value into another type. Casting <span class="k">double → int</span> truncates; the reverse just adds decimal places.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Cast <em>before</em> dividing to get a real decimal result, not after.</span></li>
        <li><span class="bul">▸</span><span><span class="k">int</span> is 32-bit — overflow past <span class="me">Integer.MAX_VALUE</span> wraps to a huge negative number, silently.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">int</span> total = <span class="n">7</span>, count = <span class="n">2</span>;</span>
      <span class="cl">System.out.<span class="me">println</span>(total / count); <span class="c">// 3 — truncated</span></span>
      <span class="cl"><span class="k">double</span> average = (<span class="k">double</span>) total / count;</span>
      <span class="cl">System.out.<span class="me">println</span>(average); <span class="c">// 3.5 — correct</span></span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.05-casting-and-ranges-of-values.md` (CSA 1.5)
