# Compound Assignment Operators

**Outline ref:** 2.5 — Compound Assignment Operators (CSA 1.6)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 2.5 · compound.assignment()</span>
  <h2>Compound <span class="y">Assignment Operators</span></h2>
  <p class="lead">Shortcuts that do the math and the assignment in one step — <span class="k">x += 1</span> is exactly <span class="k">x = x + 1</span>.</p>
  <div class="code">
    <span class="cl"><span class="k">int</span> shotCount = <span class="n">0</span>;</span>
    <span class="cl">shotCount++;&nbsp;&nbsp;<span class="c">// same as shotCount = shotCount + 1</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="k">double</span> flywheelSpeed = <span class="n">1.0</span>;</span>
    <span class="cl">flywheelSpeed *= <span class="n">0.9</span>;&nbsp;&nbsp;<span class="c">// spin down by 10%</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl">loopCount %= <span class="n">20</span>;&nbsp;&nbsp;<span class="c">// wraps back to 0 every 20 cycles</span></span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.06-compound-assignment-operators.md` (CSA 1.6)
