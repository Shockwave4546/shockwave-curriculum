# Assignment & Input

**Outline ref:** 2.3 — Assignment and Input (CSA 1.4)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 2.3 · assignment.and.input()</span>
  <h2>Assignment & <span class="y">Input</span></h2>
  <p class="lead">The assignment operator <span class="k">=</span> means "gets," not "equals" — the expression on the right is evaluated first, then stored in the variable on the left.</p>
  <div class="code">
    <span class="cl"><span class="k">int</span> encoderTicks = <span class="n">0</span>;</span>
    <span class="cl">encoderTicks = encoderTicks + <span class="n">1</span>; <span class="c">// "gets its old value, plus one"</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="c">// Every variable must be assigned before use, from a compatible type:</span></span>
    <span class="cl"><span class="k">double</span> gain = <span class="n">0.1</span>;</span>
    <span class="cl"><span class="k">int</span> whoops = gain; <span class="c">// compile error — a double doesn't fit in an int</span></span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.04-assignment-and-input.md` (CSA 1.4)
