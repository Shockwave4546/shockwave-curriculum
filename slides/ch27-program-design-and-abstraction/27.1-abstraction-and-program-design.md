# Abstraction and Program Design

**Outline ref:** 27.1 — Abstraction and Program Design (CSA 3.1)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 27.1 · abstraction.and.design()</span>
  <h2>Abstraction & <span class="y">Program Design</span></h2>
  <div class="two">
    <div>
      <p class="lead">Before writing a subsystem, find the nouns: a robot with an <span class="t">Intake</span>, a <span class="t">Shooter</span>, a <span class="t">Drivetrain</span> — each becomes a class, hiding its wiring behind public methods.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span><strong>Procedural abstraction:</strong> call <span class="me">intake.extend()</span> without knowing which solenoid channel it flips.</span></li>
        <li><span class="bul">▸</span><span><strong>Data abstraction:</strong> a <span class="t">Pose2d</span> is just "x, y, heading" — you never touch its internal matrix.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="c">// The method name IS the abstraction —</span></span>
      <span class="cl"><span class="c">// no caller needs to know how it's done</span></span>
      <span class="cl">intake.<span class="me">extend</span>();</span>
      <span class="cl">shooter.<span class="me">spinUpTo</span>(kShooterRPM);</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-3-class-creation/3.01-abstraction-and-program-design.md` (CSA 3.1)
