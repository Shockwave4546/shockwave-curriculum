# Inheritance Hierarchies

**Outline ref:** 17.3 — Inheritance Hierarchies (CSA 5.5)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 17.3 · inheritance.hierarchies()</span>
  <h2>Inheritance <span class="y">Hierarchies</span></h2>
  <div class="two">
    <div>
      <p class="lead">A superclass-typed variable, array, or list can hold any of its subclasses — but never the reverse. This is why a mixed fleet of subsystems can share one list.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span><span class="t">Subsystem[] all = { new Intake(), new Climber() };</span> — both are-a <span class="t">Subsystem</span>.</span></li>
        <li><span class="bul">▸</span><span>Which overridden method actually runs is decided by the object's real type, not the variable's declared type.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="t">ArrayList</span>&lt;<span class="t">SubsystemBase</span>&gt; subsystems = <span class="k">new</span> <span class="t">ArrayList</span>&lt;&gt;();</span>
      <span class="cl">subsystems.<span class="me">add</span>(<span class="k">new</span> <span class="t">Intake</span>());</span>
      <span class="cl">subsystems.<span class="me">add</span>(<span class="k">new</span> <span class="t">Climber</span>());</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-5-inheritance-optional/5.05-inheritance-hierarchies.md` (CSA 5.5)
