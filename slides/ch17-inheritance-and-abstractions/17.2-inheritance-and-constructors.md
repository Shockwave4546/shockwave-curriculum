# Inheritance & Constructors

**Outline ref:** 17.2 — Inheritance and Constructors (CSA 5.2)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 17.2 · inheritance.and.constructors()</span>
  <h2>Inheritance & <span class="y">Constructors</span></h2>
  <p class="lead">Constructors aren't inherited — a subclass must call <span class="k">super(...)</span> to run its parent's constructor and initialize the fields it can't touch directly.</p>
  <div class="code">
    <span class="cl"><span class="k">public class</span> <span class="t">SwerveModule</span> <span class="k">extends</span> <span class="t">BaseSubsystem</span> {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">public</span> <span class="me">SwerveModule</span>(<span class="k">int</span> canId) {</span>
    <span class="cl">&nbsp;&nbsp;&nbsp;&nbsp;<span class="k">super</span>(canId); <span class="c">// runs BaseSubsystem's constructor first</span></span>
    <span class="cl">&nbsp;&nbsp;}</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-5-inheritance-optional/5.02-inheritance-and-constructors.md` (CSA 5.2)
