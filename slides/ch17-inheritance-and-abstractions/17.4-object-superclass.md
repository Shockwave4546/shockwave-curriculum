# Object Superclass

**Outline ref:** 17.4 — Object Superclass (CSA 5.7)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 17.4 · object.superclass()</span>
  <h2><span class="y">Object</span> Superclass</h2>
  <p class="lead">Every class in Java is a subclass of <span class="t">Object</span>, whether it says <span class="k">extends</span> or not — that's where <span class="me">toString()</span> and <span class="me">equals()</span> come from by default.</p>
  <div class="code">
    <span class="cl"><span class="k">public</span> <span class="t">String</span> <span class="me">toString</span>() {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">return</span> <span class="s">"Pose("</span> + x + <span class="s">", "</span> + y + <span class="s">")"</span>; <span class="c">// overriding Object's default</span></span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-5-inheritance-optional/5.07-object-superclass.md` (CSA 5.7)
