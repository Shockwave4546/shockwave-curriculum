# Objects — Instances of Classes

**Outline ref:** 4.3 — Objects – Instances of Classes (CSA 1.12)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 4.3 · objects.instances()</span>
  <h2>Objects <span class="y">— Instances of Classes</span></h2>
  <p class="lead">A class is a blueprint; an <strong>object</strong> is one specific thing built from it. One <span class="t">TalonFX</span> class, many motor objects — each with its own CAN ID and state.</p>
  <div class="code">
    <span class="cl"><span class="c">// TalonFX is the class — leftMotor and rightMotor are objects (instances)</span></span>
    <span class="cl"><span class="t">TalonFX</span> leftMotor;</span>
    <span class="cl"><span class="t">TalonFX</span> rightMotor;</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.12-objects-instances-of-classes.md` (CSA 1.12)
