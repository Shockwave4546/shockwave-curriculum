# Creating & Initializing Objects

**Outline ref:** 4.5 — Creating and Initializing Objects: Constructors (CSA 1.13)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 4.5 · creating.objects()</span>
  <h2>Creating & <span class="y">Initializing Objects</span></h2>
  <p class="lead">Use <span class="k">new</span> plus a call to a <strong>constructor</strong> to build an object. A class can offer more than one constructor — <strong>overloaded</strong> — for different ways to set it up.</p>
  <div class="code">
    <span class="cl"><span class="t">TalonFX</span> leftMotor = <span class="k">new</span> <span class="t">TalonFX</span>(<span class="n">5</span>); <span class="c">// CAN ID 5</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="c">// Argument order matters — this constructor is (x, y, rotation)</span></span>
    <span class="cl"><span class="t">Pose2d</span> start = <span class="k">new</span> <span class="t">Pose2d</span>(<span class="n">1.0</span>, <span class="n">2.0</span>, <span class="k">new</span> <span class="t">Rotation2d</span>());</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.13-creating-and-initializing-objects-constructors.md` (CSA 1.13)
