# Calling Class Methods

**Outline ref:** 4.2 — Calling Class Methods (CSA 1.10)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 4.2 · calling.class.methods()</span>
  <h2>Calling <span class="y">Class Methods</span></h2>
  <p class="lead">A <span class="k">static</span> method belongs to the class itself, not to any object — call it with <span class="t">ClassName</span>.<span class="me">methodName()</span>, no object needed.</p>
  <div class="code">
    <span class="cl"><span class="c">// Math is a class full of static methods — no "new Math()" needed</span></span>
    <span class="cl"><span class="k">double</span> distance = <span class="t">Math</span>.<span class="me">abs</span>(target - current);</span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="c">// A non-void method returns a value — you must use it</span></span>
    <span class="cl"><span class="k">double</span> hyp = <span class="t">Math</span>.<span class="me">sqrt</span>(a * a + b * b);</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.10-calling-class-methods.md` (CSA 1.10)
