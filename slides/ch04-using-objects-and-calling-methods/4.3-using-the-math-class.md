# Using the Math Class

**Outline ref:** 4.3 — Using the Math Class (CSA 1.11)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 4.3 · math.class()</span>
  <h2>Using the <span class="y">Math Class</span></h2>
  <div class="two">
    <div>
      <p class="lead"><span class="t">Math</span> is a built-in library of static methods — no object, no <span class="k">new</span>, just call it directly.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span><span class="me">Math.abs()</span>, <span class="me">Math.pow()</span>, <span class="me">Math.sqrt()</span> — all static, all on the AP reference sheet.</span></li>
        <li><span class="bul">▸</span><span><span class="me">Math.random()</span> returns <span class="n">[0.0, 1.0)</span> — multiply and cast to get a random int in any range.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="c">// Random autonomous delay between 0 and 4 seconds</span></span>
      <span class="cl"><span class="k">double</span> delay = <span class="t">Math</span>.<span class="me">random</span>() * <span class="n">4.0</span>;</span>
      <span class="cl">&nbsp;</span>
      <span class="cl"><span class="c">// Random int in range 1-10, inclusive</span></span>
      <span class="cl"><span class="k">int</span> pick = (<span class="k">int</span>)(<span class="t">Math</span>.<span class="me">random</span>() * <span class="n">10</span>) + <span class="n">1</span>;</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.11-using-the-math-class.md` (CSA 1.11)
