# String Algorithms

**Outline ref:** 6.2 — String Algorithms (CSA 2.10)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 6.2 · string.algorithms()</span>
  <h2>String <span class="y">Algorithms</span></h2>
  <p class="lead">Loop through a string character by character with <span class="me">.substring(i, i+1)</span> — the standard pattern for scanning, searching, and building up a new string.</p>
  <div class="code">
    <span class="cl"><span class="k">int</span> count = <span class="n">0</span>;</span>
    <span class="cl"><span class="k">for</span> (<span class="k">int</span> i = <span class="n">0</span>; i < autoName.<span class="me">length</span>(); i++) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">if</span> (autoName.<span class="me">substring</span>(i, i + <span class="n">1</span>).<span class="me">equals</span>(<span class="s">"-"</span>)) count++;</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.10-implementing-string-algorithms.md` (CSA 2.10)
