# Strings

**Outline ref:** 6.1 — Strings (CSA 1.15)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 6.1 · strings()</span>
  <h2><span class="y">Strings</span></h2>
  <div class="two">
    <div>
      <p class="lead">A <span class="t">String</span> is immutable — once created, it never changes. Every method that "modifies" a string actually returns a brand-new one.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>Compare strings with <span class="me">.equals()</span>, never <span class="k">==</span> — that only checks if they're the same object.</span></li>
        <li><span class="bul">▸</span><span><span class="me">.length()</span>, <span class="me">.substring()</span>, <span class="me">.indexOf()</span> are the workhorses — all indexed from 0.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="t">String</span> autoName = <span class="s">"BlueAlliance-Left"</span>;</span>
      <span class="cl">System.out.<span class="me">println</span>(autoName.<span class="me">equals</span>(<span class="s">"BlueAlliance-Left"</span>)); <span class="c">// true</span></span>
      <span class="cl">System.out.<span class="me">println</span>(autoName.<span class="me">indexOf</span>(<span class="s">"Left"</span>)); <span class="c">// 13</span></span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.15-strings.md` (CSA 1.15)
