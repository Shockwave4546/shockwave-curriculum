# Comparing Boolean Expressions (De Morgan's Laws)

**Outline ref:** 5.7 — Comparing Boolean Expressions (De Morgan's Laws) (CSA 2.6)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.7 · de.morgans.laws()</span>
  <h2>Comparing Boolean Expressions <span class="y">(De Morgan's Laws)</span></h2>
  <p class="lead">Negating a compound condition flips both the operator and each comparison — a shortcut for writing the "opposite" of a complex check without double-negatives.</p>
  <div class="code">
    <span class="cl"><span class="c">// !(a && b)  is the same as  !a || !b</span></span>
    <span class="cl"><span class="c">// !(a || b)  is the same as  !a && !b</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="c">// "not (ready AND armed)" simplifies to:</span></span>
    <span class="cl"><span class="k">if</span> (!ready || !armed) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="me">abort</span>();</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.06-comparing-boolean-expressions-de-morgan-s-laws.md` (CSA 2.6)
