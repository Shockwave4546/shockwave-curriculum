# Nested Iteration

**Outline ref:** 5.10 — Nested Iteration (CSA 2.11)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.10 · nested.iteration()</span>
  <h2>Nested <span class="y">Iteration</span></h2>
  <p class="lead">A loop inside a loop — the inner loop runs to completion for <em>every single pass</em> of the outer one. Perfect for anything grid-shaped.</p>
  <div class="code">
    <span class="cl"><span class="k">for</span> (<span class="k">int</span> row = <span class="n">0</span>; row < <span class="n">8</span>; row++) {</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">for</span> (<span class="k">int</span> col = <span class="n">0</span>; col < <span class="n">8</span>; col++) {</span>
    <span class="cl">&nbsp;&nbsp;&nbsp;&nbsp;visionGrid[row][col] = <span class="me">sampleCell</span>(row, col);</span>
    <span class="cl">&nbsp;&nbsp;}</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.11-nested-iteration.md` (CSA 2.11)
