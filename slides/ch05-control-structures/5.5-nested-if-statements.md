# Nested if Statements

**Outline ref:** 5.5 — Nested if Statements (CSA 2.4)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.5 · nested.if.statements()</span>
  <h2>Nested <span class="y">if Statements</span></h2>
  <p class="lead">Chain <span class="k">else if</span> branches to pick between 3+ paths — only the first true branch runs, and a trailing <span class="k">else</span> catches everything else.</p>
  <div class="code">
    <span class="cl"><span class="k">if</span> (distance < <span class="n">1.0</span>) {</span>
    <span class="cl">&nbsp;&nbsp;state = <span class="t">IntakeState</span>.<span class="t">CLOSE</span>;</span>
    <span class="cl">} <span class="k">else if</span> (distance < <span class="n">3.0</span>) {</span>
    <span class="cl">&nbsp;&nbsp;state = <span class="t">IntakeState</span>.<span class="t">MID</span>;</span>
    <span class="cl">} <span class="k">else</span> {</span>
    <span class="cl">&nbsp;&nbsp;state = <span class="t">IntakeState</span>.<span class="t">FAR</span>;</span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.04-nested-if-statements.md` (CSA 2.4)
