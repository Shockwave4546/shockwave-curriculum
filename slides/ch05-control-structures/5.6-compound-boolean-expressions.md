# Compound Boolean Expressions

**Outline ref:** 5.6 — Compound Boolean Expressions (CSA 2.5)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.6 · compound.boolean.expressions()</span>
  <h2>Compound <span class="y">Boolean Expressions</span></h2>
  <div class="two">
    <div>
      <p class="lead">Combine conditions with <span class="k">&&</span> (and), <span class="k">||</span> (or), and <span class="k">!</span> (not) — Java short-circuits, skipping the right side once the answer is already known.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span><span class="k">&&</span> needs both sides true; <span class="k">||</span> needs just one.</span></li>
        <li><span class="bul">▸</span><span>Short-circuiting is a real safety net: <span class="k">sensor != null && sensor.isFaulty()</span> never touches a null sensor.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">if</span> (armReady && intakeReady) {</span>
      <span class="cl">&nbsp;&nbsp;scoreCommand.<span class="me">schedule</span>();</span>
      <span class="cl">}</span>
      <span class="cl">&nbsp;</span>
      <span class="cl"><span class="k">if</span> (sensor != <span class="k">null</span> && sensor.<span class="me">isFaulty</span>()) {</span>
      <span class="cl">&nbsp;&nbsp;<span class="me">report</span>(sensor);</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.05-compound-boolean-expressions.md` (CSA 2.5)
