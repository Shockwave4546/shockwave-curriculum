# Selection & Iteration Algorithms

**Outline ref:** 5.9 — Implementing Selection and Iteration Algorithms (CSA 2.9)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.9 · selection.iteration.algorithms()</span>
  <h2>Selection & Iteration <span class="y">Algorithms</span></h2>
  <p class="lead">The <strong>accumulator pattern</strong> is the workhorse of loop-based algorithms: initialize a variable, update it every pass, use the final value once the loop ends.</p>
  <div class="code">
    <span class="cl"><span class="k">double</span> sum = <span class="n">0</span>, max = <span class="t">Double</span>.<span class="me">MIN_VALUE</span>;</span>
    <span class="cl"><span class="k">for</span> (<span class="k">double</span> sample : visionLatencies) {</span>
    <span class="cl">&nbsp;&nbsp;sum += sample; <span class="c">// accumulate</span></span>
    <span class="cl">&nbsp;&nbsp;<span class="k">if</span> (sample > max) max = sample; <span class="c">// running max</span></span>
    <span class="cl">}</span>
    <span class="cl"><span class="k">double</span> avgLatency = sum / visionLatencies.<span class="me">size</span>();</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.09-implementing-selection-and-iteration-algorithms.md` (CSA 2.9)
