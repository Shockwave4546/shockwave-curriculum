# Boolean Expressions

**Outline ref:** 5.4 — Boolean Expressions (CSA 2.2)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.4 · boolean.expressions()</span>
  <h2>Boolean <span class="y">Expressions</span></h2>
  <div class="two">
    <div>
      <p class="lead">Relational operators compare values and always produce <span class="k">true</span> or <span class="k">false</span> — the currency every <span class="k">if</span> and loop condition runs on.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>One <span class="k">=</span> assigns; two <span class="k">==</span> compares. Mixing these up is a classic bug.</span></li>
        <li><span class="bul">▸</span><span><span class="k">==</span> on objects checks if they're the <em>same object</em> — not "equal," use <span class="me">.equals()</span> for that.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">double</span> battery = <span class="n">11.8</span>;</span>
      <span class="cl">System.out.<span class="me">println</span>(battery < <span class="n">12.0</span>); <span class="c">// true — low battery</span></span>
      <span class="cl">System.out.<span class="me">println</span>(battery >= <span class="n">12.0</span>); <span class="c">// false</span></span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.02-boolean-expressions.md` (CSA 2.2)
