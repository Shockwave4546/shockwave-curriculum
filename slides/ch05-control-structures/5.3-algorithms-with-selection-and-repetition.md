# Algorithms with Selection & Repetition

**Outline ref:** 5.3 — Algorithms with Selection and Repetition (CSA 2.1)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.3 · selection.and.repetition()</span>
  <h2>Algorithms with <span class="y">Selection & Repetition</span></h2>
  <p class="lead">Every algorithm is built from just three building blocks: <strong>sequence</strong> (do things in order), <strong>selection</strong> (branch on a true/false decision), and <strong>repetition</strong> (loop until done).</p>
  <div class="fgrid three">
    <div class="fcard no-bracket"><div class="flabel">Sequence</div><div class="ftitle">Step by step</div><div class="fdesc">Code runs top to bottom by default.</div></div>
    <div class="fcard no-bracket"><div class="flabel">Selection</div><div class="ftitle">Branching</div><div class="fdesc">if/else — pick a path based on a condition.</div></div>
    <div class="fcard no-bracket"><div class="flabel">Repetition</div><div class="ftitle">Looping</div><div class="fdesc">Repeat a block while a condition holds.</div></div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.01-algorithms-with-selection-and-repetition.md` (CSA 2.1)
