# Informal Runtime Analysis of Loops

**Outline ref:** 5.11 — Informal Runtime Analysis of Loops (CSA 2.12)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.11 · runtime.analysis()</span>
  <h2>Informal Runtime Analysis <span class="y">of Loops</span></h2>
  <p class="lead">You can predict how many times a loop runs without executing it: <span class="k">largest − smallest + 1</span>. Nested loops multiply — outer count × inner count.</p>
  <div class="code">
    <span class="cl"><span class="k">for</span> (<span class="k">int</span> i = <span class="n">3</span>; i < <span class="n">7</span>; i++) { ... } <span class="c">// runs 6-3+1 = 4 times</span></span>
    <span class="cl">&nbsp;</span>
    <span class="cl"><span class="c">// 5 outer passes × 10 inner passes = 50 total iterations</span></span>
    <span class="cl"><span class="k">for</span> (<span class="k">int</span> row = <span class="n">0</span>; row < <span class="n">5</span>; row++)</span>
    <span class="cl">&nbsp;&nbsp;<span class="k">for</span> (<span class="k">int</span> col = <span class="n">0</span>; col < <span class="n">10</span>; col++) { ... }</span>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.12-informal-runtime-analysis-of-loops.md` (CSA 2.12)
