# While Loops

**Outline ref:** 5.8 — While Loops (CSA 2.7)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 5.8 · while.loops()</span>
  <h2>While <span class="y">Loops</span></h2>
  <div class="two">
    <div>
      <p class="lead">A <span class="k">while</span> loop repeats its body as long as a condition holds — checked <em>before</em> every pass, including the first.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>3 steps, every time: initialize the loop variable, test it, update it inside the loop.</span></li>
        <li><span class="bul">▸</span><span>Forget step 3 and you get an infinite loop — the classic robot-code freeze.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">int</span> attempts = <span class="n">0</span>; <span class="c">// 1. initialize</span></span>
      <span class="cl"><span class="k">while</span> (attempts < <span class="n">3</span> && !connected) { <span class="c">// 2. test</span></span>
      <span class="cl">&nbsp;&nbsp;connected = <span class="me">tryConnect</span>();</span>
      <span class="cl">&nbsp;&nbsp;attempts++; <span class="c">// 3. update</span></span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-2-selection-and-iteration/2.07-while-loops.md` (CSA 2.7)
