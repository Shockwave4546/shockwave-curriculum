# Commands & Command Compositions

**Outline ref:** 25.2 — Commands & Command Compositions (WPILIB 20.2-3)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 25.2 · commands.and.compositions()</span>
  <h2>Commands & <span class="y">Command Compositions</span></h2>
  <div class="two">
    <div>
      <p class="lead">A <span class="t">Command</span> has 4 lifecycle methods: <span class="me">initialize()</span>, <span class="me">execute()</span>, <span class="me">end()</span>, <span class="me">isFinished()</span>. Chain simple commands into bigger ones with <strong>compositions</strong> — sequences, parallels, races.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>A command declares its <span class="me">getRequirements()</span> so the scheduler never runs two commands on the same subsystem at once.</span></li>
        <li><span class="bul">▸</span><span>Compositions are themselves commands — they nest recursively.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="c">// Run fooCommand, then race barCommand against bazCommand</span></span>
      <span class="cl">button.<span class="me">onTrue</span>(fooCommand.<span class="me">andThen</span>(barCommand.<span class="me">raceWith</span>(bazCommand)));</span>
    </div>
  </div>
</div>
```

Derived from `other-reference-repo`: `wpilib/command-based/02-commands.md`, `03-command-compositions.md` (WPILIB 20.2-3)
