# The Command Scheduler

**Outline ref:** 25.3 — The Command Scheduler (WPILIB 20.8)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 25.3 · the.command.scheduler()</span>
  <h2>The <span class="y">Command Scheduler</span></h2>
  <p class="lead">One singleton, called every 20ms from <span class="me">robotPeriodic()</span> — it polls triggers, schedules commands, runs every scheduled command's <span class="me">execute()</span>, and ends the ones that finished or got interrupted.</p>
  <div class="code">
    <span class="cl"><span class="k">public void</span> <span class="me">robotPeriodic</span>() {</span>
    <span class="cl">&nbsp;&nbsp;<span class="t">CommandScheduler</span>.<span class="me">getInstance</span>().<span class="me">run</span>(); <span class="c">// forget this and NOTHING runs</span></span>
    <span class="cl">}</span>
  </div>
</div>
```

Derived from `other-reference-repo`: `wpilib/command-based/08-command-scheduler.md` (WPILIB 20.8)
