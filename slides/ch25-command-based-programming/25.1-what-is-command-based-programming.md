# What Is Command-Based Programming?

**Outline ref:** 25.1 — What Is Command-Based Programming? (WPILIB 20.1)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 25.1 · what.is.command.based()</span>
  <h2>What Is <span class="y">Command-Based Programming?</span></h2>
  <p class="lead">A declarative pattern built on two core abstractions: <strong>Commands</strong> (actions the robot can take) and <strong>Subsystems</strong> (hardware groupings). Say "run this when that happens" instead of hand-checking button state every 20ms.</p>
  <div class="code">
    <span class="cl"><span class="c">// Declarative: say what should happen, not how to poll for it</span></span>
    <span class="cl"><span class="k">new</span> <span class="t">Trigger</span>(driver::getAButton)</span>
    <span class="cl">&nbsp;&nbsp;.<span class="me">onTrue</span>(<span class="t">Commands</span>.<span class="me">runOnce</span>(intake::<span class="me">extend</span>));</span>
  </div>
</div>
```

Derived from `other-reference-repo`: `wpilib/command-based/01-what-is-command-based.md` (WPILIB 20.1)
