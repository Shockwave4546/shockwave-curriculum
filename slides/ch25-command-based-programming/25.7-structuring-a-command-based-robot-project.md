# Structuring a Command-Based Robot Project

**Outline ref:** 25.7 — Structuring a Command-Based Robot Project (WPILIB 20.4,6-7)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 25.7 · structuring.a.project()</span>
  <h2>Structuring a Command-Based <span class="y">Robot Project</span></h2>
  <div class="two">
    <div>
      <p class="lead">A standard command-based project splits into <span class="t">Robot</span> (control flow), <span class="t">RobotContainer</span> (button bindings, wiring), <span class="t">Constants</span>, plus <span class="me">subsystems/</span> and <span class="me">commands/</span> packages.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>A <strong>Subsystem</strong> hides hardware behind public methods — the rest of the codebase never touches a motor controller directly.</span></li>
        <li><span class="bul">▸</span><span><span class="t">RobotContainer</span> is where declarative wiring lives: which trigger schedules which command.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">public class</span> <span class="t">Intake</span> <span class="k">extends</span> <span class="t">SubsystemBase</span> {</span>
      <span class="cl">&nbsp;&nbsp;<span class="k">public</span> <span class="t">Command</span> <span class="me">extendCommand</span>() {</span>
      <span class="cl">&nbsp;&nbsp;&nbsp;&nbsp;<span class="k">return</span> <span class="me">runOnce</span>(() -&gt; solenoid.<span class="me">set</span>(kForward));</span>
      <span class="cl">&nbsp;&nbsp;}</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `other-reference-repo`: `wpilib/command-based/04-subsystems.md`, `06-structuring-command-based-project.md`, `07-organizing-command-based.md` (WPILIB 20.4,6-7)
