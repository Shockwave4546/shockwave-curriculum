# Intro to Algorithms, Programming & Compilers

**Outline ref:** 1.2 — Intro to Algorithms, Programming and Compilers (CSA 1.1)
**Status:** new — authored teaser slide, in the deck's existing HTML/CSS-class format

```html
<div>
  <span class="tag">// 1.2 · first.java.program()</span>
  <h2>Your First <span class="y">Java Program</span></h2>
  <div class="two">
    <div>
      <p class="lead">Every Java program lives inside a <span class="t">class</span>, and starts running at its <span class="me">main</span> method. Every statement ends with a semicolon <span class="k">;</span> — miss one, and the compiler stops you before the code ever runs.</p>
      <ul class="bullets">
        <li><span class="bul">▸</span><span>A <strong>compiler</strong> checks your code for mistakes and translates it into something that can actually run.</span></li>
        <li><span class="bul">▸</span><span>A mistake the compiler catches is a <strong>syntax error</strong> — you'll see plenty of these while you're learning, and that's normal.</span></li>
      </ul>
    </div>
    <div class="code">
      <span class="cl"><span class="k">public class</span> <span class="t">MyClass</span> {</span>
      <span class="cl">&nbsp;&nbsp;<span class="k">public static void</span> <span class="me">main</span>(<span class="t">String</span>[] args) {</span>
      <span class="cl">&nbsp;&nbsp;&nbsp;&nbsp;System.out.<span class="me">println</span>(<span class="s">"Hi there!"</span>);</span>
      <span class="cl">&nbsp;&nbsp;}</span>
      <span class="cl">}</span>
    </div>
  </div>
</div>
```

Derived from `csawesome-2026`: `unit-1-using-objects-and-methods/1.01-introduction-to-algorithms-programming-and-compilers.md` (CSA 1.1)
